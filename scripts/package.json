{
  "name": "xclusiveline-image-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-off migration of product images from Supabase Storage to Cloudflare R2",
  "scripts": {
    "migrate": "node migrate-images.mjs",
    "migrate:dry": "node migrate-images.mjs --dry-run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.637.0"
  }
}
